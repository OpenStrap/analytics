// DEPRECATED module. Its former contents are now split across spec-aligned files:
//   - HR recovery (HRR60) → src/recovery.ts (calcHrRecovery)
//   - Sleep regularity (SRI) → src/regularity.ts (calcSleepRegularity)
//   - Stress → BANNED (see docs/CONFIDENCE.md); removed entirely.
// Nothing is exported here. Kept as an empty module to avoid stale imports.
export {};
