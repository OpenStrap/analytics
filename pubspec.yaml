name: openstrap_analytics
description: Pure-Dart 1:1 port of openstrap-analytics (published-algorithm analytics over minute rollups).
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.5.0

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
  openstrap_protocol:
    path: ../openstrap-protocol-dart
